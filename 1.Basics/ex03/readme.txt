ex03 : 전통적인 DOM 기반의 application: js module system을 사용한 파일 분리 II

1. ES6 module system을 지원하는 브라우저에서는 es6 module system 기반으로 분리된 js 모듈(파일)간의 의존성을 보장 해준다.
2. javascript 외의 다양한 asset들이 존재...(css, font, image)의 로딩 동기화는 불가능하다......!! (단점!!)